# Compatibility evidence

There are zero qualified records. The catalog is an inventory artifact, not a
runtime allowlist reader. No boolean or JSON edit here can enable product writes.
The current Rust availability function refuses every mutation unconditionally.

A future reviewed adapter must implement exact-build binding, complete policy
reading, safe resource ownership, lifecycle proof and evidence validation before
consuming qualified records. Synthetic fixtures must never be placed in this
catalog. Keep owner-run raw evidence outside the repository in ignored
`local-evidence/`; publish only reviewed, sanitized evidence references.

Use the fields and tests in SPEC-COMPATIBILITY and T-28. Do not invent a native
Desktop build, resource set, signature validation or successful account roundtrip.
