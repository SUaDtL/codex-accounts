# CA-02: T-07 wording proposal

Status: proposal ready for owner review; not adopted into the normative source.
No compatibility or operation permission is granted by this document or renderer.

The original architecture permits a positively established, exact-build official
file default, while the original T-07 says only explicit file mode proceeds.
The candidate includes both explicit and positively established default-file
fixtures and explicitly refuses missing-setting inference, auto, keyring,
unsupported sources and unresolved precedence. No other normative text changes.

## Reproduce the candidate

```console
python tools/spec_proposal.py --check
python tools/spec_proposal.py --preview > ca-02-spec-review.html
python tools/spec_proposal.py --manifest-preview > ca-02-provenance-review.json
```

These commands operate on repository documentation, not account files. Preview
outputs are generated review artifacts, not a second working specification. Do not
commit them as parallel live sources. The current normative HTML and its source
manifest remain unchanged pending owner adoption of this bounded amendment.

This is an explicit proposal-delivery variation from the packet's anticipated
in-place specification edit. P02-A08 has deterministic candidate and migration
regression evidence, but adoption itself is still pending. Do not describe the
new default-file wording as already merged or use it to enable an adapter.

The renderer changes the canonical T-07 row and its visible row together. Product
version stays 0.1.0; document-revision metadata identifies the proposal. Tests
reverse only T-07 and document metadata and require the exact original byte digest.
Every modeled section and source note is checked against its visible projection.

The proposed v2 source-input manifest retains `original_sha256`, current `sha256`,
revision and this amendment reference. Original research/index hashes are retained.
A later adoption commit must apply the generated candidate and manifest together,
update the working-plan basis and move the pending-proposal check to an adopted
revision check. Neither proposal status nor a hash is a qualification signature.

Original SHA-256:
`a5041d63e0b517f68019a4b527c050014616e7bd46c5742768a65199120ad3e6`.
Candidate SHA-256:
`af8171ed2e69cdca214afc5a569f060e70582033c72829594c1c81f02fd82d43`.

The source-level gate and empty catalog stay in place. All-positive test inputs
cannot enable capture, login or switching. Pending Q0 evidence does not prevent
separately bounded generic CA-03 development, but blocks real integration.
