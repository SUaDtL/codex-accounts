# Selected preparation review: CA-05A, CA-06A and macOS

Review: [PR #12](https://github.com/SUaDtL/codex-accounts/pull/12), branch
`feat/ca-05-06-macos-preparation`, based on owner-merged PR #11 at
`455baa2eeee614586b26d419ee961475fe1ad23b`. Refresh its head, comments and checks
before editing. No matching open PR existed at session start; this review was
opened for the owner's three explicitly selected independent preparations.
The unfinished CA-04D branch/checkouts are separate and were not overwritten.

## Delivered preparation, not full phases

| Track | Implementation and review boundary |
| --- | --- |
| CA-05A | `crates/runtime` adds terminal EOF, exact outbound method-shape classification and fourteen adversarial integration tests. No official schema, process/helper launch, login, refresh or credential IO. See `ca-05a-protocol-preparation.md`. |
| CA-06A | `app/` contains deterministic synthetic account, status and recovery HTML, local CSS and sixteen structural/accessibility/escaping tests. Operational actions are disabled. No command integration or renderer consent; not the finished application. See `../app/README.md`. |
| macOS preparation | `macos/native-adapter-packet.md` records Apple documentary facts, seven pinned shared source interfaces and a bounded synthetic-native return contract. Nine source tests check drift and reject false authority. No native implementation, behavior pass or qualification. |

The source tests join existing mandatory discovery; runtime tests join existing
workspace debug/release and isolated Linux execution. No new dependency, manifest,
workflow filter, skipped-test exemption, native adapter or compatibility record.
The single roadmap remains `implementation-plan.html`; all full requirements and
T-01 through T-34 release scenarios remain open.

## Continuation gates

Finish this review against its final combined head. Source-test success does not
substitute for the browser/keyboard/zoom and screen-reader checks marked NOT RUN in
`app/README.md`; it also does not qualify any native platform. Inspect hosted source,
all Rust lanes, exact named Windows behavior proof and the isolated Linux gate.
Do not inherit a green result from an earlier head.

Operational CA-05 still requires Q0/Q2, a reviewed exact executable/schema contract
and owned-helper lifecycle. Operational CA-06 requires Q2/Q3 and native consent;
static metadata cannot authorize either. Keep malformed JSON and schema validation
explicitly deferred rather than interpreting raw frames as verified responses.

Before macOS native coding, resolve the separate CA-04D staging-repair continuation,
review the final shared interfaces and reissue the packet's baseline in review.
A hash-only update to silence drift is not approval. The next bounded native scope
is synthetic filesystem/home coordination only, with exact SDK/binding review and
an authorized macOS arm64 test host. Windows evidence is not macOS evidence.

Read `maintainers/preparation-handoff.md`, `maintainers/parallel-work.md`,
`maintainers/open-asks.md` and live AGENTS.md. One integrator owns shared interfaces,
module wiring and planning. No automatic advancement, merge, force push/reset,
release, permissions change or live account operation.
